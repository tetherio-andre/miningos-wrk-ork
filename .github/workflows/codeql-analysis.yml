# CodeQL for orgs that require actions from repos they own (e.g. tetherio-andre).
# Prereq: Fork github/codeql-action -> tetherio-andre/codeql-action and tag v3.

name: CodeQL Analysis

on:
  push:
    branches: [main]
  schedule:
    - cron: "0 2 * * *"  # Run at 2 AM UTC daily

jobs:
  analyze:
    name: Analyze Code
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: write
      contents: read

    steps:
      - name: Checkout repository
        uses: tetherio-andre/checkout@v6

      - name: Initialize CodeQL
        uses: tetherio-andre/codeql-action/init@v3
        with:
          languages: javascript

      - name: Autobuild
        uses: tetherio-andre/codeql-action/autobuild@v3

      - name: Perform CodeQL Analysis
        uses: tetherio-andre/codeql-action/analyze@v3
